// FormBuilder constants

// Example constants (adjust based on your actual needs)
export const FIELD_TYPES = {
  TEXT: 'text',
  NUMBER: 'number',
  EMAIL: 'email',
  SELECT: 'select',
  CHECKBOX: 'checkbox',
  RADIO: 'radio',
  DATE: 'date',
  TEXTAREA: 'textarea',
};

export const DEFAULT_FIELD_CONFIG = {
  required: false,
  disabled: false,
};
